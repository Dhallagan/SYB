import { getUser, listUsers, createUser, updateUser, deleteUser } from './users';

// Now you can use these functions in your index.js file
export {
    getUser,
    listUsers,
    createUser,
    updateUser,
    deleteUser
}